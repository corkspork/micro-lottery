# Micro Lottery Frontend

To get started, clone this repo and follow these commands:

1. Run `yarn install` at the root of your directory
2. Run `yarn run start` to start the project
3. Start coding!
